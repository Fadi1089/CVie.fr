// Seed script — will be populated with template configs in later stories
async function main() {
  console.log("Seed complete (no data to seed yet)");
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
