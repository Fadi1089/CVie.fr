// TODO (deferred): populate once Vitest + React Testing Library land in the
// client workspace. Target coverage:
//   - clicking the card fires onSelect with the template id
//   - isSelected adds `aria-pressed="true"` and the selected ring style
//   - aria-label uses the template name
//   - focus-visible ring appears on keyboard focus
//   - badge renders "Compatible ATS" text (icon alone must not convey meaning)
//
// Tracking: `_bmad-output/implementation-artifacts/deferred-work.md` → story 2.1.
export {};
